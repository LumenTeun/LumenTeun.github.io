{
  "name": "ahstro.github.io",
  "version": "3.0.0",
  "repository": "git@github.com:ahstro/ahstro.github.io.git",
  "author": "Anton Strömkvist <hi@ahst.ro>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "csso-cli": "^1.0.0",
    "elm-css": "^0.6.0",
    "uglify-js": "^3.0.27"
  },
  "scripts": {
    "build:dev": "elm make src/Main.elm --output build/index.js && elm-css src/Stylesheets.elm --output build/ ; exit 0",
    "build:prod": "yarn build:dev && uglifyjs --mangle --compress --output build/index.js -- build/index.js && csso -i build/index.css -o build/index.css",
    "watch": "chokidar '**/*.elm' -c 'yarn build:dev'"
  }
}
